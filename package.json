{
  "scripts": {
    "prepare": "husky",
    "install:all": "npm i && npm i --prefix backend && npm i --prefix ./backend/users" 
  },
  "devDependencies": {
    "@commitlint/cli": "^19.4.1",
    "@commitlint/config-conventional": "^19.4.1",
    "husky": "^9.1.5"
  }
}
